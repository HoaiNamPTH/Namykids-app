# NamyKids App — Migration Gate Memo v1.0

Date: 2026-09-21
Target development database: Supabase `Namykids app` / `oohseaqfwvekrwyfelrf`
Production impact: NONE — App has not launched and this project began with 0 tables / 0 migrations / 0 production data.

## Gate decision

**NOT READY — NARROW BLOCKER**

The database design and non-production dry-run are technically healthy, but the Migration Gate is not fully approvable yet because the canonical migration file has not been generated by Supabase CLI and live Web-token verification has not been exercised with a real test session through the App backend boundary.

## VERIFIED FACT — Dry-run migrations

Applied on the App development project:
1. `20260921143703_dryrun_app_runtime_foundation_v1`
2. `20260921143824_dryrun_app_runtime_advisor_hardening_v1`

These are dry-run migration-history entries on the development App project. They are **not** the canonical repo migration artifact.

## VERIFIED FACT — Schema

Created and verified:
- app.identity_binding
- app.entitlement_snapshot
- app.device_registration
- content.content_release
- content.content_node_version
- app.activity_attempt
- app.activity_result
- app.progress_projection
- app.resume_pointer
- private.domain_outbox
- public.commit_activity_completion(...)
- public.revoke_app_identity(...)

RLS is enabled on all App/content/private tables.
Direct table privileges for anon/authenticated are revoked.
Explicit deny policies exist for anon/authenticated.
Completion/revocation functions are not executable by anon/authenticated and are restricted to service_role.

## VERIFIED FACT — Dry-run tests PASS

Passed:
1. direct mobile runtime grants denied
2. Completion Commit execute denied to anon/authenticated
3. two-active-App-device limit enforced
4. published release immutability
5. published content-node immutability
6. Completion Commit successful atomic path
7. duplicate completion ID is idempotent
8. wrong-child request rejected with zero canonical writes
9. FULL-required activity fails closed under LIMITED entitlement
10. child revocation blocks new completion writes
11. account revocation removes active binding/device/entitlement access and creates outbox reconciliation event

Test data was wrapped in transactions and rolled back.

## VERIFIED FACT — Advisors

### Security advisor
**0 findings after hardening.**

Initial RLS-without-policy INFO findings were removed by adding explicit deny policies for mobile roles.

### Performance advisor
No missing-FK-index findings remain after hardening.

Remaining findings are only **unused_index / INFO**, expected on a newly created database with no production workload. These indexes cover planned FK/query paths and are retained until real workload evidence exists.

## VERIFIED FACT — Type generation

Supabase TypeScript generation succeeded for the current exposed/default schema surface.
The generated type output includes the public RPC signatures:
- commit_activity_completion
- revoke_app_identity

Custom App/content/private table schemas are intentionally not part of the default public Data API surface.

## AUTH test status

### VERIFIED
The Web project remains the identity authority and exposes a modern publishable key.
The approved token-verification contract uses server-side Web Supabase Auth validation (`getUser(token)` / equivalent) as the baseline, so it does not require Web service-role credentials.

### NOT YET VERIFIED END-TO-END
A real Web test session has not yet been sent through an implemented App backend endpoint in this environment.

Required later test:
- valid Web token → accepted
- expired/corrupted token → rejected
- token from unrelated project → rejected
- verified Parent A + Child B → rejected

## Canonical migration blocker

Current ChatGPT execution environment does not have Supabase CLI and cannot download/install it because outbound shell DNS/network is unavailable.

Therefore:
- no timestamped migration filename was invented manually;
- no file was falsely labeled as CLI-canonical;
- the approved next execution owner is Codex/local worktree with Supabase CLI.

Canonicalization steps:
1. clone/open `HoaiNamPTH/Namykids-app`
2. install/verify current stable Supabase CLI
3. `supabase init` if needed
4. create migration through `supabase migration new <descriptive_name>`
5. consolidate the tested dry-run DDL + advisor hardening into that CLI-created file
6. run local/preview reset/dry-run
7. rerun migration test plan
8. verify migration list
9. commit migration + tests
10. return commit SHA for CMO review

## Legacy progress cutover condition

Before release, Web legacy:
- lesson_progress
- game_sessions
- activity_completions

must be explicitly classified as migrated / archived-read-only / retired so only the App project remains writable progress authority.

This is a release/cutover condition and does not invalidate the current schema dry-run.

## Gate conclusion

Database schema design: **PASS**
RLS/API hardening: **PASS**
Completion Commit/idempotency: **PASS**
Device rule: **PASS**
Deletion/revocation propagation: **PASS**
Security advisor: **PASS**
Performance advisor: **PASS WITH EXPECTED UNUSED-INDEX INFO**
Canonical CLI migration artifact: **BLOCKED**
Live cross-project Auth integration test: **PENDING**

**Migration Gate = NOT READY — NARROW BLOCKER**

No APP-ARCH decision is reopened.
